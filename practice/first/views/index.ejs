<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="POST">
                <input class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none " type="text" placeholder="Title goes here" name="title">
                <textarea class="block px-5 mt-2 py-2 bg-zinc-800 w-full rounded-md outline-none resize-none" placeholder="Write your task details here..." name="details"></textarea>
                <button type="submit" class="mt-2 p-3 bg-green-500 text-white rounded-md">Create Task</button>
            </form>
        </div>

        <div class="tasks p-10 flex gap-3 flex-wrap p-10">

            <% if(files.length > 0){%>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                        <div class="flex w-full justify-between items-center mt-3">
                            <a class="text-blue-500 inline-block " href="/file/<%= val %>" >Read More</a>
                            <a href="/edit/<%= val %>" class="text-zinc-600">edit</a>
                        </div>
                    </div>
                <% }) %>
            <% } else {%>
                <h3 class="text-zinc-600">no tasks</h3>
            <% }%>

            
        </div>
    </div>
</body>
</html>
let DEBUG_MODE,Joi,CustomErrorHandler;_1d4‍.x([["default",()=>_1d4‍.o]]);_1d4‍.w("../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_1d4‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_1d4‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);



const errorHandler = (err, req, res, next) => {
    let statusCode = 500;
    let data = {
        message: 'Internal server error',
        ...(DEBUG_MODE === 'true' && { originalError: err.message })
    };

    if (err instanceof Joi.ValidationError) {
        statusCode = 400;
        data = {
            message: err.message
        };
    }
    
    if (err instanceof CustomErrorHandler) {
        statusCode = err.status;
        data = {
            message: err.message
        };
    }

    return res.status(statusCode).json(data);
};

_1d4‍.d(errorHandler);
